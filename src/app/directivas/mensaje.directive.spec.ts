import { MensajeDirective } from './mensaje.directive';

describe('MensajeDirective', () => {
  it('should create an instance', () => {
    const directive = new MensajeDirective();
    expect(directive).toBeTruthy();
  });
});
